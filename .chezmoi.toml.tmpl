# chezmoi's own knobs. This template generates ~/.config/chezmoi/chezmoi.toml
# on `chezmoi init` — the documented way for chezmoi to manage its own config
# (a plain managed file would race apply against config reload).
[cd]
    command = "fish"
